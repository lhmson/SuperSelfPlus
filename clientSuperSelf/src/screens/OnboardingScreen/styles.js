import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  dots: {
    height: 8,
    marginHorizontal: 3,
    borderRadius: 5,
  },
  actionButton: {
    marginHorizontal: 10,
  },
});

export default styles;
